https://www.sudo.ws/getting/download/

rpm -qa | grep sudo
rpm -e sudo-1.9.5p2-1.ppc
mkdir /tmp/sudo; chmod 777 /tmp/sudo
cd /tmp/sudo
smitty install 
visudo
ansible         ALL=(ALL) NOPASSWD: ALL
su - ansible
sudo su -



AIX supports three main file systems: JFS, JFS2, and NFS.

AIX performance monitoring tools include NMON, topas, vmstat, iostat, and sar. 
utilities help identify bottlenecks, monitor resource usage, and optimize system performance.

What is the role of Corosync ?
How to check your Cluster status?
~]# pcs cluster status
~]# pcs status

pcs cluster enable --all

