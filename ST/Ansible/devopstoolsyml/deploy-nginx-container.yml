---
- hosts: all
  gather_facts: yes

  tasks: 
    - name: pull the nginx image
      command: docker pull nginx:latest
    
    - name: run nginx container
      command: docker run -d -p 9080:80 --name nginx-server nginx:latest
    
    - debug: 
        msg= "nginx container started successfully"
