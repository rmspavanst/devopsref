---
- hosts: all
  gather_facts: no

  tasks:
    - name: Test URL
      win_uri:
        url: http://localhost
        status_code: 200     
        validate_certs: no
      register: urlResult

    - debug:
        var: urlResult
